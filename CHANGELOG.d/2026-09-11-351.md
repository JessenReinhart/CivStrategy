- **Broken saved settings no longer get loaded into a live match (#351):** Continue now rejects corrupted saved tax-rate or bloom values before replacing the world, while older version-1 saves that never stored those optional settings still load normally.
